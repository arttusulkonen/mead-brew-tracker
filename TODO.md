### 📁 TODO.md — Ingria Brewcraft PWA

#### 🏗️ Этап 1: Инициализация проекта и Базовая настройка

- [x] Инициализировать проект (`Vite` + `React` + `TypeScript`).
- [x] Установить базовые зависимости (`react-router-dom`, `zustand`, `recharts`, `date-fns`, `react-icons`).
- [x] Создать масштабируемую глобальную SCSS-архитектуру (`_variables.scss`, `_base.scss`, `_layout.scss`, миксины).
- [x] Настроить PWA плагин (`vite-plugin-pwa`) для генерации манифеста и Service Worker.

#### 🌐 Этап 2: Настройка Интернационализации (i18n)

- [x] Установить пакеты: `i18next`, `react-i18next`, `i18next-parser`.
- [x] Создать единый конфигурационный файл `languages.json` для централизованного управления языками.
- [x] Создать файл конфигурации парсера `i18next-parser.config.cjs`.
- [x] Настроить инициализацию `i18next` в `src/i18n.ts` с поддержкой автоматического сохранения выбора в `localStorage`.
- [x] Написать скрипты автоматического перевода батчами через Gemini и выгрузки в БД.
- [x] Реализовать полностью адаптивный переключатель языков в `Navigation.tsx`.

#### 🔥 Этап 3: Интеграция БД и Auth (Переезд на Supabase)

- [x] Выбор СУБД: Успешный переезд с Firebase на надежную реляционную базу данных Supabase (PostgreSQL).
- [x] Реляционное проектирование: Создание монолитного скрипта инициализации схем `database_init.sql` (таблицы `recipes`, `ingredients`, `styles`, `inventory`).
- [x] Безопасность данных: Настройка строгих политик RLS (Row Level Security) для разграничения доступа пользователей в рамках multi-tenant архитектуры.
- [x] Настроить авторизацию по Email/Паролю и глобальный стор аутентификации (`useAuthStore`).
- [x] Разработать страницы авторизации и регистрации (`Login.tsx`, `Register.tsx`) с защитой роутов.

#### 👥 Этап 4: Разработка логики Рабочих пространств (Collaboration)

- [x] Создать коллекцию `breweries` (Рабочие пространства) в Supabase.
- [x] Реализовать логику автоматического создания "Личной пивоварни" (Personal Brewery) при регистрации.
- [x] Разработать UI в Профиле для отображения списка доступных пивоварен с возможностью переключения.
- [x] Реализовать функционал создания новой "Совместной пивоварни" (Shared Brewery).
- [x] Разработать логику генерации email-инвайтов и атомарный обработчик вступления пользователей при логине.
- [x] Реализовать функцию каскадного удаления пивоварни владельцем вместе со всеми связанными данными.

#### 🧪 Этап 5: Разработка UI-модулей по технической спецификации

##### 📦 Подмодуль 5.1: База Ингредиентов и Склад

- [x] Разработка двухколоночного интерфейса инвентаря в Apple-style с поддержкой динамических форм под категории.
- [x] Кастомные модальные селекторы: Создание переиспользуемых компонентов `IngredientSearchModal` и `StyleSearchModal` для быстрого поиска с фильтрацией (вместо стандартных `<select>`).
- [x] Глобальная база сырья: Разработка и успешный запуск скрипта-сидера. В базу загружены эталонные стили BJCP 2021 и сотни сортов хмеля/дрожжей.
- [x] Настроить безопасные правила RLS отдельно для операций `create/update` и `delete` склада.
- [x] Логика связывания: При добавлении сырья из Глобального каталога создается локальный инстанс, позволяющий переопределять метрики (например, YAN) под конкретную закупленную партию.

##### 🥼 Подмодуль 5.2: Лаборатория (Рецепты)

- [x] Снимок характеристик (Snapshotting): Отвязка рецепта от жесткой зависимости от склада. Ингредиенты внутри рецепта хранят свои локальные копии параметров.
- [x] Валидация стилей на клиенте: Внедрение `bjcpMatchEngine.ts` для интерактивного контроля за рамками стиля (OG, FG, ABV, IBU) прямо в процессе сборки.
- [x] Интегрировать математическое ядро (`@mead-tracker/math`) и калькулятор TOSNA 3.0 (динамический расчет OG, ABV, питания дрожжей).
- [x] Smart Additive Calculator (TOSNA): Умная панель, автоматически распределяющая добавки на основе их роли (`Rehydration` vs `Fermentation`).
- [x] Разработать UI-конструктор "Шагов варки" с независимыми фазами (Preparation/Fermentation/Aging) и гибким State Machine (поддержка откатов).
- [x] Умные рекомендации: Блок автоматического подбора дрожжей и хмеля под выбранный стиль на основе полей `best_for` и `notes`.
- [ ] Разработать логику **Pro-Scaling**: масштабирование рецепта от 10 л до промышленных объемов с автоматическим нелинейным пересчетом параметров.
- [ ] Реализовать функцию агрегации и отображения статистики на странице рецепта (количество варок, средний ABV).

##### ⏱️ Подмодуль 5.3: Режим Варки (Live Brew Engine)

- [x] Реализовать транзакционное вычитание ингредиентов из инвентаря склада при запуске варки в `BrewSessionSetup`.
- [x] Реализовать реактивный интерфейс `TimelineStepper` на базе БЭМ-модели для управления статическими шагами варки.
- [x] Разработать событийную модель таймеров в `BrewSession` с защитой от сброса времени при перезагрузке страницы.
- [x] Добавить модальное окно "Actual OG" для фиксации реальной плотности сусла перед засевом дрожжей.
- [x] Интегрировать интерактивный виджет `TOSNA 3.0 Tracker` в боковую колонку. Автоматическое перестроение графика при достижении _1/3 Sugar Break_.
- [x] Разработать функцию инлайн-комментариев и быстрых записей логов (_Quick Note_) внутри таймлайна.
- [x] **Split Batch Backend**: Транзакционный пайплайн разделения партии с архивацией родителя.
- [x] **Split Batch Frontend**: UI-модальное окно разделения объемов и вызов функции `splitBatch`.
- [ ] Безопасное списание сырья: Перевод логики списания остатков на строгие SQL-транзакции Supabase с защитой от отрицательных остатков.
- [ ] Фоновые уведомления: Добавление push-уведомлений (PWA) при срабатывании будильников на внесение хмеля или пауз затирания.

##### 📊 Подмодуль 5.4: Дневник и Аналитика Брожения

- [x] Создать независимую атомарную подколлекцию `fermentation_logs` внутри документов сессий.
- [x] Разработать _Live Feedback Engine_: система моментальных детерминированных подсказок (уведомление о критическом падении pH < 3.2).
- [x] Реализовать интерактивный график `AdvancedFermentationChart` на Recharts (двухосевое отображение кривой SG и pH).
- [ ] Наложение графиков: Реализовать функцию наложения и сравнения графиков брожения текущего батча с прошлыми успешными варками (Golden Batches).

#### 🛠️ Этап 6: Стабилизация после миграции (Технический Долг) 📍 МЫ ЗДЕСЬ

- [ ] **Математический аудит:** Восстановление и жесткая калибровка калькулятора TOSNA 3.0 и расчетов OG в пакете `@mead-tracker/math` до эталонных значений старой Firebase-версии. Написание юнит-тестов для формул.
- [ ] **Синхронизация типов:** Приведение типов ID (строгий `uuid` вместо микса с `text`) к единому стандарту во всех таблицах, RLS-политиках и TypeScript-интерфейсах.
- [ ] **Аудит глобального стейта (Zustand):** Рефакторинг сторов (`useAuthStore`, `useBreweryStore`, `useInventoryStore`) — устранение дублирующихся состояний (`loading` vs `isLoading`), исправление зависимостей от `activeBreweryId`.
- [ ] **Возврат Strict Mode:** Включение строгих правил в `tsconfig.app.json` (`strict: true`, `noImplicitAny: true`) и устранение `any` в компонентах для предотвращения runtime-ошибок при работе с инвентарем и логами варки.

#### 🤖 Этап 7: Интеграция ИИ (Smart Brew Assistant & Recipe Wizard)

- [x] Создать базу знаний эталонных рецептов для механизма _Few-Shot Prompting_.
- [x] Настроить пайплайн _Math-to-AI_: детерминированный расчет объемов на клиенте -> передача жестких чисел в промпт ИИ -> генерация шагов варки.
- [x] Установить зависимости: `genkit`, `@genkit-ai/google-genai` для ИИ-бэкенда.
- [x] **AI Rate Limiting / Guardrails**: Защита промптов, ограничение запросов и валидация на уровне Cloud Functions.
- [ ] Настроить векторное хранилище (RAG) для стилей BJCP, исключающее галлюцинации нейросети при сборке сложных нетипичных рецептов.
- [ ] Разработать систему кэширования успешных генераций для снижения стоимости API-запросов к LLM.
- [ ] **AI Live Brewing Assistant**: Внедрить ИИ в активную варку с текстовой интерпретацией кривой SG и предложениями по корректировке.

#### 💰 Этап 8: Коммерциализация (SaaS Features)

- [ ] Интегрировать платежный шлюз (Stripe/RevenueCat) для управления PRO-тарифами пивоварен.
- [ ] Внедрить лимиты бесплатного использования (Freemium Paywalls) на количество сохраненных рецептов и активных коллабораторов.
- [ ] Разработать модуль финансовой аналитики (COGS) — автоматический расчет себестоимости одного литра готового сусла на основе цен партий со склада.

#### 🚀 Этап 9: Тестирование и Деплой

- [ ] Протестировать PWA на мобильных устройствах (установка на экран, работа таймеров в спящем режиме, скачивание кеша).
- [ ] Интегрировать HTML5 _WakeLock API_ для автоматического предотвращения засыпания экрана смартфона во время выполнения активных шагов варки.
