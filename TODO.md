### 📁 TODO.md — Mead & Brew Tracker PWA

#### Этап 1: Инициализация проекта и Базовая настройка

- [x] Инициализировать проект (`Vite` + `React` + `TypeScript`).
- [x] Установить базовые зависимости (`react-router-dom`, `zustand`, `recharts`, `date-fns`, `react-icons`).
- [x] Создать масштабируемую глобальную SCSS-архитектуру (`_variables.scss`, `_base.scss`, `_layout.scss`, миксины).
- [x] Настроить PWA плагин (`vite-plugin-pwa`) для генерации манифеста и Service Worker (исправлена конфигурация плагина совместного с React в `vite.config.ts`).

#### Этап 2: Настройка Интернационализации (i18n)

- [x] Установить пакеты: `i18next`, `react-i18next`, `i18next-parser`.
- [x] Создать единый конфигурационный файл `languages.json` для централизованного управления языками приложения.
- [x] Создать файл конфигурации парсера `i18next-parser.config.cjs` со сбором данных из `languages.json`.
- [x] Настроить инициализацию i18next в `src/i18n.ts` с поддержкой автоматического сохранения выбора в `localStorage` через `LanguageDetector`.
- [x] Написать скрипты автоматического перевода батчами через Gemini (`scripts/auto-translate.ts`) и выгрузки в Firestore (`scripts/uploadTranslations.ts`).
- [x] Реализовать полностью адаптивный переключатель языков в `Navigation.tsx` (циклическая кнопка-глобус на мобильных устройствах, полноценный `select` на десктопе).

#### Этап 3: Интеграция Firebase (Backend & Auth)

- [x] Создать проект в консоли Firebase и базу данных Firestore (с европейской локализацией).
- [x] Установить SDK и создать безопасный файл инициализации `src/firebase/config.ts` с защитой от HMR Vite.
- [x] Настроить авторизацию по Email/Паролю.
- [x] Написать глобальный стор аутентификации (`useAuthStore`) и защитить роуты через React Router.
- [x] Разработать полноценные страницы авторизации и регистрации (`Login.tsx`, `Register.tsx`) с редиректами и обработкой сессий.
- [x] Настроить Firebase Hosting: инициализировать хостинг, добавить автоматизированные скрипты `deploy:prod` для последовательной сборки и деплоя.

#### Этап 4: Разработка логики Рабочих пространств (Collaboration)

- [x] Создать коллекцию `Breweries` (Рабочие пространства) в Firestore.
- [x] Настроить жесткие Firebase Security Rules, защищающие документы пивоварен от несанкционированного чтения и перезаписи сторонними лицами (исправлена уязвимость эскалации прав обычными участниками).
- [x] Реализовать логику автоматического создания "Личной пивоварни" (Personal Brewery) при регистрации нового пользователя с использованием `serverTimestamp()`.
- [x] Разработать UI в Профиле для отображения списка доступных пивоварен пользователя с возможностью переключения активного пространства.
- [x] Реализовать функционал создания новой "Совместной пивоварни" (Shared Brewery) с валидацией и очисткой пустых строк (trim).
- [x] Разработать логику генерации email-инвайтов и атомарный обработчик `processPendingInvites` через `arrayUnion/arrayRemove` для вступления пользователей при логине.
- [x] Реализовать функцию каскадного удаления пивоварни владельцем вместе со всеми связанными рецептами и варками.

#### Этап 5: Разработка UI-модулей по технической спецификации

##### Подмодуль 5.1: База Ингредиентов и Склад

- [x] Создать глобальный каталог сырья в Firestore по пути `/ingredients` (Мед, Дрожжи, Хмель, Профили воды, Добавки).
- [x] Создать коллекцию склада конкретной пивоварни `/breweries/{id}/inventory` со ссылками на глобальные шаблоны ингредиентов и отслеживанием остатков.
- [x] Настроить безопасные правила Firestore отдельно для операций `create/update` и `delete`.
- [x] Оптимизировать загрузку данных склада и реализовать логику атомарного инкремента (обновления) `quantityOnHand` при добавлении уже существующего на складе сырья вместо создания дубликатов.
- [x] Разработать интерфейс управления запасами на складе с предпросмотром характеристик (Brix, YAN, Alpha) и визуальной подсветкой позиций, которых нет в наличии (`Out of stock`).
- [x] Интегрировать интерактивные CSS-подсказки (Tooltips) для сложных технологических параметров сырья (Brix, влажность, азотная потребность, толерантность).

##### Подмодуль 5.2: Лаборатория (Рецепты)

- [x] Написать строгие типы TypeScript для расширенных характеристик рецепта (шаги, фазы `Preparation/Fermentation/Aging`, единицы времени).
- [x] Создать коллекцию `recipes` и настроить правила безопасности Firestore на основе членства в пивоварне.
- [x] Разработать форму создания рецепта с отвязкой от конкретного инвентаря пользователя (использование глобального справочника сырья).
- [x] Интегрировать математическое ядро и калькулятор TOSNA 3.0 (динамический расчет OG, ABV, общего веса дрожжей, Go-Ferm и 4-этапного графика внесения Fermaid-O в зависимости от азотной потребности).
- [x] Разработать UI-конструктор "Шагов варки" (Brewing Steps) с независимыми фазами, полями для детальных заметок (`textarea`) и выбором единиц измерения (минуты/дни).

##### Подмодуль 5.3: Режим Варки (Live Brew Session)

- [ ] Разработать функционал **Continuous Lifecycle** (статусы сессии от Brew Day до Completed).
- [ ] Разработать интерфейс `TimelineStepper` для ведения активной варки с разделением шагов по фазам (Варка, Брожение, Выдержка) и индивидуальными таймерами.
- [ ] Реализовать режим **Freestyle Brew** (создание варки без предварительного рецепта, заполнение журнала на лету).
- [ ] Реализовать функционал **Синхронизация с реальностью**: возможность свободно редактировать шаги и массы ингредиентов внутри сессии варки (Flight Recorder).
- [ ] Разработать логику **Clone & Scale (Повторить варку)**: возможность взять историческую сессию и создать новую с динамическим пересчетом (Scaling) фактических объемов сырья на новый объем ферментера.
- [ ] Добавить функцию **Save as New Recipe** в конце варки (превращение фактического журнала успешной варки в эталонный рецепт).
- [ ] Списать сырье со склада: реализовать логику транзакционного вычитания ингредиентов из инвентаря при запуске варки.
- [ ] Написать Zustand-логику для управления PWA `WakeLock API` (блокировка отключения экрана смартфона) и фоновым аудио-плеером для удержания активных таймеров в фоне.

##### Подмодуль 5.4: Дневник и Аналитика Брожения

- [ ] Создать подколлекции `/fermentation_logs` и `/timeline_events` внутри документов сессий варок.
- [ ] Реализовать интерактивный график `AdvancedFermentationChart` на Recharts с тремя осями (Плотность, pH, Температура).
- [ ] Добавить отображение целевых безопасных диапазонов (shading) и кликабельных маркеров кастомных событий (например, внесение питания) на графике.
- [ ] Реализовать функцию сравнения батчей (наложение кривой плотности прошлых успешных варок с низкой непрозрачностью).
- [ ] Написать транзакционный пайплайн разделения партии (Split Batch) с уменьшением объема родительского сусла и глубоким копированием истории логов в дочерний документ до метки времени сплита.

#### Этап 6: Интеграция ИИ (Smart Brew Assistant)

- [ ] Установить зависимости: `genkit`, `@genkit-ai/googleai`.
- [ ] Настроить файл `src/ai/genkit.ts` и привязать серверный ключ `GOOGLE_GENAI_API_KEY`.
- [ ] Интегрировать механизм **RAG (Retrieval-Augmented Generation)**: передача `globalCatalog` (ингредиентов) в контекст AI-модели.
- [ ] Разработать генератор рецептов: парсинг пользовательского промпта естественного языка в структурированный JSON формат интерфейса `Recipe`.
- [ ] Написать функцию `analyzeFermentation(logs, recipe)` для предиктивного анализа застрявшего брожения или критического падения pH.
- [ ] Создать UI-компонент "AI Советник" в панели управления варкой.

#### Этап 7: Тестирование и Деплой

- [ ] Протестировать PWA на мобильных устройствах (установка на экран, работа таймеров в спящем режиме, скачивание кеша).
- [ ] Настроить и протестировать офлайн-режим Firestore (`enableIndexedDbPersistence`) для бесперебойной записи логов без интернета.
